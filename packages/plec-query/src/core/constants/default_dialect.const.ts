/**
 * Default SQL dialect used by the query builder.
 *
 * @remarks
 * This value is used when no dialect is explicitly configured.
 *
 * @example
 * ```ts
 * console.log(DEFAULT_DIALECT);
 * ```
 */
export const DEFAULT_DIALECT = 'postgres';
